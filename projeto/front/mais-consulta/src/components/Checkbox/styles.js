import styled from 'styled-components';
import { Checkbox as StyledCheckbox } from "@material-ui/core"
import { FormControlLabel } from "@material-ui/core";

export const CustomStyledCheckbox = styled(StyledCheckbox)`
    
`
export const CustomForm = styled(FormControlLabel)`
    display: flex !important;
    align-items: center !important;
`